# Dependencies (the plugin is dependency-free; this guards `npm install`)
node_modules/

# macOS
.DS_Store

# Editor / local
*.swp
*.log

# Runtime state lives in ~/.cache/tmux-acp-hub, never in the repo
